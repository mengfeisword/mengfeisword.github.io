Mind Map generated by NB MindMap plugin   
> __version__=`1.1`
---

# GO

## sync

### Once

#### 并发运行,只允许一次

### Locker
> align=`left`


#### 简单接口

### Mutex

#### 互斥锁

### RWMutex

#### 读写锁,多读少写,同时读锁,读写互斥

### WaitGroup

#### 分发任务,主线程等待所有任务完成

### Cond

#### 发令枪,一般预设一个条件让子任务等待,发出的信号可以是单个\(Signal\)也可集体广播\(Broadcast\)

### atomic

### Map

#### 2个场景做了优化

##### 只写1次,多次读

##### 多个goroutines读写互不相同的键\(比如goroutines1读写key1,goroutines2读写key2\)

### Pool

#### 作为临时对象的保存和复用的集合

#### 特点

##### Pool 中的对象在仅有 Pool 有着唯一索引的情况下可能会被自动删除（取决于下一次 GC 执行的时间）

##### goroutines 协程安全，可以同时被多个协程使用。

### ErrGroup

#### 在一组 Goroutine 中提供了同步、错误传播以及上下文取消的功能

### Semaphore

### SingleFlight
